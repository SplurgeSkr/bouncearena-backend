# Backend Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:8081

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
PROGRAM_ID=11111111111111111111111111111111

# Server Keypair (admin authority for submitting results)
# Generate with: solana-keygen new -o server-keypair.json
SERVER_KEYPAIR_PATH=./server-keypair.json

# Treasury Wallet (receives platform fees)
TREASURY_WALLET=YOUR_TREASURY_WALLET_PUBLIC_KEY
